import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-run", description: "Execution engine specialist", systemPrompt: "You are an execution engine specialist. Handle runtime orchestration, process management, and automated execution of tool pipelines." });
