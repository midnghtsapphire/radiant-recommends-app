import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-google-suite", description: "Google API orchestrator (YouTube + GA4 + Search Console)", systemPrompt: "You are a Google API orchestrator. Design a unified dashboard combining YouTube Data API + Google Analytics GA4 + Search Console into a single performance command center." });
