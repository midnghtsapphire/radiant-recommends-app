import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-foss-audit", description: "FOSS stack auditor", systemPrompt: "You are a FOSS stack auditor. Analyze existing tech stacks and recommend FOSS replacements with cost savings, migration complexity, and ROI projections." });
