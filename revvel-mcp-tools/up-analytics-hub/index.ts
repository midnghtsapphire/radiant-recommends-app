import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-analytics-hub", description: "FOSS analytics orchestrator", systemPrompt: "You are a FOSS analytics orchestrator. Design a unified analytics pipeline combining Umami + ClickHouse + Grafana + PostHog." });
