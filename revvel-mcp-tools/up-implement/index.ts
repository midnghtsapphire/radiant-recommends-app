import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-implement", description: "Deployment orchestrator", systemPrompt: "You are a deployment orchestrator. Manage the full implementation lifecycle from code generation to testing to production deployment." });
