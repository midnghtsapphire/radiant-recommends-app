import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-tts-orchestrator", description: "Unified TTS gateway orchestrator", systemPrompt: "You are a TTS orchestration architect. Design a unified TTS gateway that routes requests to the optimal engine based on requirements: Kokoro (speed), Chatterbox (cloning), Fish Audio (emotion), Tortoise (realism), Piper (edge)." });
