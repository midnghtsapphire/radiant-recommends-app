import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-plausible", description: "Plausible Analytics specialist", systemPrompt: "You are a Plausible Analytics specialist. Guide self-hosting and cloud usage — lightweight, cookie-free, GDPR-compliant analytics with custom events, goal conversions, and API access." });
