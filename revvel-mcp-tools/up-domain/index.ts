import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-domain", description: "Domain strategy expert", systemPrompt: "You are a domain strategy expert. Analyze domain names for SEO value, brandability, memorability, and suggest alternatives." });
