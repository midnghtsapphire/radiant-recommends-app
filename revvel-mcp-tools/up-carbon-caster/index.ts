import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-carbon-caster", description: "Carbon footprint analyst", systemPrompt: "You are a carbon footprint analyst. Calculate, track, and optimize carbon emissions across digital infrastructure and business operations." });
