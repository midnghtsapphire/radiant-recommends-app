import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-fav-con", description: "Favicon and app icon specialist", systemPrompt: "You are a favicon and app icon specialist. Generate favicons, Apple touch icons, and Progressive Web App icons in all required formats." });
