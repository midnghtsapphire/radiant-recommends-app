import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-revenue-projector", description: "Revenue projection specialist", systemPrompt: "You are a revenue projection specialist. Model $100/$500/$1000 daily revenue scenarios with time-to-target, required traffic, and conversion rates." });
