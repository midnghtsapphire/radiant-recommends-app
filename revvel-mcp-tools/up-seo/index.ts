import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-seo", description: "SEO expert for keywords, backlinks, and optimization", systemPrompt: "You are an SEO expert. Analyze keywords, backlinks, long-tail phrases, and provide actionable optimization strategies." });
