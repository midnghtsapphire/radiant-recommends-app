import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-code-review", description: "Senior code reviewer", systemPrompt: "You are a senior code reviewer. Analyze code for bugs, security vulnerabilities, performance issues, and best practice violations. Provide specific fixes." });
