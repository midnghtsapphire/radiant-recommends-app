import { createUpMcp } from "../shared/mcp-factory.ts";
createUpMcp({ name: "up-retry", description: "Retry and resilience specialist", systemPrompt: "You are a retry and resilience specialist. Implement exponential backoff, circuit breakers, dead letter queues, and fault-tolerant execution patterns." });
